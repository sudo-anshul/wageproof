> Historical local release-verification report. Current repository/video status is in the [README](../README.md) and [publication notes](PUBLICATION.md). Media and report paths named below belong to the preserved full local handoff, not this source repository.

# WageProof local upgrade — lexhack-requests-2

The upgrade completes the evidence-request step and repairs misleading case-detail fallbacks. It is running locally at port 4319 with all three existing saved cases preserved. Nothing was pushed, deployed, uploaded, sent or submitted.

## Improvements

- **Usable request drafts.** Current open/partly answered records requests have a focused recipient/subject/body editor, the exact original scope and source citations. Wording saves locally, reloads and exports as exact saved UTF-8 text. The deterministic starting text is disclosed for editing; no new model call is made.
- **Reliable revision boundaries.** Drafts retain their source/revision/action basis. New evidence, a new revision, a changed request or active analysis prevents stale export. Concurrent saves reject silently overwriting another edit, and the editor preserves typed text through errors. Saving never sends a message, answers a request or marks the wage account reviewed.
- **Honest unknowns.** Removing names, recipient, claim number or claim status no longer resurrects intake values in the selected account or exports. Historical values remain in their own revisions. Claim status is editable through the existing correction workflow.
- **Clearer recovery and narrow layouts.** Unreadable cases say Needs recovery and show the preservation message without false zero counts or dates. Source inspection, focus return, dirty-exit protection and request controls were inspected on desktop, 390px and 320px layouts. The accepted cobalt design remains.

## Actual verification

The new source archive passed **124/124 tests**, fresh installation/inspection doctor/build and **30 setup/runtime checks**. All 8 rebuilt assets matched. The 154 archive members / 111 frozen product files and 79 source links plus 6 built links were checked. A bounded credential-pattern scan found no match; it is not comprehensive security assurance.

Ten recorded browser observation groups cover real save/reload/download/copy, failure/retry, competing saved versions, unrelated case conflicts, new-revision and new-source staleness, source inspection, keyboard focus, narrow prepared preview and context/recovery labels. The original browser review names candidate 1; candidate 2 changes only an obsolete test assertion and release-document labels. Runtime source and asset hashes are identical. New feature capture uses candidate 2.

The existing demonstration's 15 export comparisons stayed byte-identical. The $140→$80→$80 account, final $55/$25 weekly differences, single correction and outstanding payroll reconciliation remain unchanged. The prior public model evaluation is reused with its original limits; the ordinary workflow also passed. No new model accuracy, superiority, human saving, practitioner validation or competition score is claimed.

The independent review reproduced a Unicode-control field validation error and verified its repair while preserving the failed attempt. The first fresh archive also retained one obsolete test expectation; the second passed the full suite. Browser harness failures, earlier visual defects and their repair receipts remain recorded.

Activation at 4319 matched the frozen runtime source/assets. All 27 saved files across the three existing cases remained byte-identical; no active analysis was interrupted. Fictional testing used separate storage.

## Deliverables and remaining dependencies

The source archive (`source/wageproof-source.zip`), complete manifest/freeze, [fresh setup record](UPGRADE-REQUESTS-SETUP.md), [request guide](REQUEST-DRAFTS.md), [submission fields](SUBMISSION-REQUESTS.md), public evaluation, browser evidence and preserved failures are included in the handoff.

The existing 174-second core recording-ready cut, captions, timed human script, thumbnail and editable media remain preserved. It is silent by design because the user's human narration is not ready. A separate real request-workflow demonstration (retained in the full local handoff), capture review (retained in the full local handoff), captions, thumbnail, raw footage and local render recipe accompany the upgrade. They do not extend the three-minute core master. The first capture had clipped footer controls; that failed cut is preserved with the review and the corrected capture is supplied as the final supplement. Human speech fitting, caption synchronization, listening and the final narrated export remain pending.

The final thumbnail was reframed after the earlier independent technical receipt. The thumbnail addendum (retained in the full local handoff) verifies its exact final image and crop; the earlier receipt and first assembled handoff remain preserved. Source, video, captions and saved text did not change for this package correction.

The user confirmed a solo student entry. Actual profile/age/original-work attestations, a public repository or live link, a hosted video and Devpost submission remain pending. Deadline: **28 September 2026 at 02:30 Asia/Kolkata** (27 September 17:00 EDT). Current organizer guidance asks for the creator's human pitch. No AI voice was substituted and no automation was resumed.

Scope remains English records and the declared ordinary California hourly calculation slice. The product does not decide legal entitlement or case closure. Request text is user-editable, unsent and requires judgment. Setup evidence is from the provisioned macOS host, not a clean OS or hosted multi-user deployment. The previous completed release and all earlier failures remain separate, unchanged artifacts.
