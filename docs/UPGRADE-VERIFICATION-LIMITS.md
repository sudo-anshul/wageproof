# Verification scope and portable evidence

The product source and build are frozen as **upgrade-candidate-4**. The candidate manifest and the separately supplied final source manifest bind those bytes. This source ZIP includes setup instructions and selected reports; the fresh install/build result for this exact archive is supplied with the accompanying local handoff, avoiding a circular archive hash inside itself.

Read the [implemented changes](UPGRADE-IMPLEMENTATION.md), [final synthetic evaluation](../evaluation/upgrade/public/FINAL-EVALUATION.md) and [compact result summary](../evaluation/upgrade/public/FINAL-SUMMARY.json). Evaluation results apply to the declared synthetic cases and configuration. They do not establish worker impact, legal accuracy, agency acceptance, comparative human effort or a competition score.

The [independent integration report](UPGRADE-INDEPENDENT-INTEGRATION-REVIEW.md) retains four original defects and their repair checks. The [diagnostic review](UPGRADE-DIAGNOSTIC-REVIEW.md) retains the first exact-quote retry, short-update omission and ordinary-workflow infrastructure failures, plus the subsequent completed ordinary diagnostic review. The [export repair](UPGRADE-EXPORT-REPAIR.md) records general renderer changes and actual first-page inspection. Failed artifacts were preserved; an exposed rerun is not unseen evidence.

The [UX report](UPGRADE-UX-REVIEW.md) includes desktop/narrow/dialog/source checks and preserved failure screenshots. Its last follow-up inspects the fresh fictional demonstration with the current product assets. These are bounded developer inspections, not a complete screen-reader audit or participant study. The [earlier setup report](UPGRADE-CLEAN-SETUP-PREVIOUS.md) covers candidate 1 only: its 75-test result is historical. The upgraded suite has 80 tests; current exact-source execution belongs to the external setup report.

The [candidate equality receipt](UPGRADE-CANDIDATE-EQUALITY.json) records that candidate 3 amended comparator tooling while retaining candidate 2's application/build bytes. The [candidate 4 delta](UPGRADE-CANDIDATE4-DELTA.json) binds its subsequent renderer/baseline-projection/test changes; client build assets remain unchanged. Earlier equality does not apply to the changed server renderer.

The included ordinary evaluation runner uses the recorded native permission profile. Protected non-temp input/key/history boundaries were probed. The installed sandbox permits access under /private/tmp despite requested deny entries, so no global work-only-write or temp-secrecy guarantee is claimed. A [specific staging probe](UPGRADE-TEMP-STAGING-QUALIFICATION.json) found ordinary-model access to the app's observed macOS temporary directory denied, and candidate 4 uses [protected non-temp extraction scratch](UPGRADE-CANDIDATE4-TMPDIR.json). No contamination was observed in the inspected transcripts. Model inference uses the configured remote provider. Raw keys, corpus, case stores and provider logs are excluded; these runners are audit tooling, not a standalone distributed benchmark.

The [preparation provenance](UPGRADE-PACKAGE-PROVENANCE.json) binds prepared copies to authored reports and explicitly lists omitted raw artifacts. Its preparation status describes that earlier step. Final source additions and final archive hashes are recorded in the accompanying source manifest. Omission is a portability decision, never deletion or reclassification of a failure.

Human narration remains a separate step. The separately delivered silent recording-ready film is not a voiced or submitted entry. Everything in this handoff was prepared locally.
