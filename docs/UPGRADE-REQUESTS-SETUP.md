# Exact-source setup — lexhack-requests-2

The final source archive passed a fresh extraction, installation, inspection doctor, all **124 tests** (zero failures or skips), production build and **30 setup/runtime checks**. All **8 rebuilt assets** were byte-identical to the frozen build. The archive has 154 members and 111 frozen product files.

SHA-256: `0e3638adc9d87d471954edcd9f1e2b334062f9df3f0e18c40f7832a681b6d776`.

The isolated runtime had only Node and Python on PATH. It inspected the recorded fictional example and current request preview without inference, checked exact JSON/TXT equality, refused prepared-case mutation, preserved historical source selection across three evidence ZIPs and verified all six original files. The empty case/job directory stayed unchanged. The owned service exited normally with no descendants or open port.

The first archive is preserved separately:123/124 tests passed, while an old assertion expected a missing selected-revision name to fall back to intake. That assertion contradicted the adopted correction behavior. Its replacement verifies `Not supplied` and absence of the old intake name. A new archive received the complete fresh check; the failed attempt is not counted as success.

This was the existing provisioned macOS host (Node 22.20.0), not a clean operating system. npm may use the host cache/network. Linux/Windows, live model service access, legal validity and human benefit are not established by these checks. Full-suite transport fixtures are distinct from model evaluation. Browser and media checks are recorded separately.

The handoff's `reports/setup/` directory contains exact command receipts, full test/build logs, the independent review, final verification and preserved first failure. Its `source/` directory contains the archive, complete manifest and frozen identity. No credentials, user cases or private evaluation answers are included.
