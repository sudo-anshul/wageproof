# WageProof finishing release — lexhack-finish-1

The existing wage-correction workflow and accepted cobalt design are preserved. This finishing pass makes the recorded demonstration accessible without model credentials, clarifies setup and case identity, and reconciles the local release materials. It does not add a new jurisdiction, model or claim of superiority.

## Implemented

- A fixed bundled fictional case opens through **Inspect the prepared example**. Its six original records and three recorded revisions retain their original identities, dates, source snapshots and review notes. The prepared alias is read-only; all non-GET requests for it fail before mutation. Inspection creates no jobs or saved cases.
- The loader verifies bounded regular files, expected paths, byte hashes, exact text normalization, schema/citations scoped to each revision, and recomputed accounting. Missing/corrupt data fails explicitly. It strips provider/job/cache fields and recomputes comparisons.
- Prepared downloads carry the recorded-example origin and technical-review limitation, including the selected revision's actual review note. Ordinary exports retain their existing behavior. Quotes and hashes establish traceability, not factual truth, legal entitlement or document authenticity.
- The desk separates recorded inspection from fresh analysis. Missing Codex disables analysis while intake and existing results remain available, with a local recheck. Fictional workspaces show saved times and a stable short reference when those labels collide. Failed navigation clears the old record; a successful retry clears the error.
- `npm run doctor -- --inspection` requires Node and Python but treats Codex as optional. Default doctor continues to require Codex. Neither sends a model request.

## Release and evidence boundaries

The fixed example derives from the known candidate4 fictional demonstration: $140 → $80 → $80; one $60 gross correction and $48 net receipt; final $55/$25 weekly differences; payroll reconciliation and recipient details remain unresolved. It is recorded output, never a fresh evaluation. Review markers document a technical demonstration, not worker/practitioner approval.

The previous synthetic evaluation remains unchanged: candidate3's primary short-output failures, candidate4's known-state export repair and the eight fresh reserve targets remain separate. The strong ordinary workflow also passed the fresh targets. No unconsumed reserve, human time-saving measurement, adoption study or legal validation is claimed. Model/prompt/schema/accounting code remains unchanged from candidate4.

Current execution receipts are supplied in the separate final release and setup reports, because an archive cannot contain a report of testing its own final hash. Earlier reports retained in this archive are historical and identify their releases. Tests of prepared loading and presentation are not new model evidence.

## Architecture and operational boundaries

React/Vite client → loopback Node service → local source/version store. Configured Codex performs fresh interpretation; deterministic domain code validates and calculates. The prepared viewer uses a fixed verified bundle with the same domain validation/export machinery. It accepts no arbitrary case-JSON import. The service keeps host/origin checks and private store permissions; it has no public multi-user authentication.

Fresh analysis sends extracted text and context to the configured service and takes minutes in recorded runs; monetary cost remains unknown. Original files and case storage stay local. Scope remains English records and a declared ordinary California hourly slice. The fixed example works without Codex; fresh unfamiliar-document analysis still needs configured service access. macOS is the exercised platform, Linux unverified, Windows unsupported by the POSIX store lock.

## Demo and submission

The existing 174-second picture edit shows real candidate4 live-workflow captures. It does not depict a new run or the new fixed viewer. Its hashes, scene coverage and ordinary-case compatibility are checked separately. Human narration is not ready yet, as confirmed by the user; the silent cut, timed script, captions, thumbnail and editable handoff remain recording-ready. No AI voice is substituted.

The user reports being a student and entering solo. Actual participant/profile and age eligibility remain to confirm against the official form. The current deadline is 28 September 2026 at 02:30 Asia/Kolkata. Official rules require a public repository or live URL and a hosted video of at most three minutes; the organizer requests a genuine human pitch. Nothing is pushed, deployed, uploaded or submitted. Existing recurring automation remains paused.
