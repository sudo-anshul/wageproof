// Vercel's public function exposes only the bundled fictional walkthrough.
// The editable local server is deliberately not imported or started here.
export { default } from '../server/hosted-demo.mjs';
